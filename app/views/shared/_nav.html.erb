  <nav class="main-nav">
  <div class="logo">
    <a href="<%= root_url %>" class="logo">
    <%= image_tag "angry_cat.png" %>
    F*** That Noise</a>
  </div>

  <ul class="nav-items">
    <% if logged_in? %>
    <li>
      <a href="#"><%= current_user.username %></a>
      <ul class="dropdown-nav hidden">
        <li><a href="<%= user_url(current_user) %>">Profile</a></li>
        <li><%= button_to "Sign Out", session_url, method: :delete %></li>
      </ul>
    </li>
    <% else %>
    <li><%= link_to "Sign In", new_session_url %></li>
    <% end %>
    <li><%= link_to "About", about_url %></li>
    <li><%= link_to "Contact", contact_url %></li>
  </ul>
  <a href="#" class="nav-dropdown"><i class="fa fa-bars" aria-hidden="true"></i></a>
</nav>

<div class="tagline">
  <h3>Find actions and events that make a difference</h3>
</div>
